$(document).ready(function() {
    $('#trainingForm').on('submit', function(event) {
        event.preventDefault();
        // Codul pentru trimiterea formularului
        alert('Formularul a fost trimis cu succes!');
    });
});